<script>
	$(function() {
		$( ".tablesorter" ).tablesorter( {
		    headers: { 
	            1: { sorter: false },
	            6: { sorter: false }
	        }, 
			sortList: [[2,0]]
		} ); 
	});
</script>
[% IF !session.isAdmin %]
  		<div class="info info-rouge">
			<h1>Accès refusé</h1>
			<p>Cette section est réservée aux administrateurs. <a href="[% request.uri_base %]/">Cliquez-ici</a> pour retourner à l'application.</p>
		</div>
[% END %]
[% IF session.isAdmin %]
 		<div class="info info-vert">
			<h1>Information</h1>
			<p>Voici la liste des dépôts non expirés</p>
		</div>
[% USE date %]
[% USE String %]
[% USE format %]
[% USE arrondi = format('%02.2f') %]
		<table class="tablesorter" cellspacing="1">
			<thead>
			<tr>
				<th>Propriétaire</th>
				<th>Numéro du dépôt</th>
				<th>Taille du dépôt</th>
				<th>Date de création</th>
				<th>Date d'expiration</th>
				<th>Téléchargé</th>
				<th>Actions</th>
			</tr>	
			</thead>	
			<tbody>	
			[% FOREACH deposit = liste_deposit %]
			[% created_date = String.new(deposit.created_date).replace('T', ' ') %]
			[% expiration_date = String.new(deposit.expiration_date).replace('T', ' ') %]
			[% deposit_size = 0 %]
			<tr>
				<td>[% deposit.id_user.login %]</td>
				<td>[% deposit.id_deposit %]</td>
				<td>[% FOREACH file IN deposit.files %][% deposit_size = deposit_size + file.size %][% END %][% IF deposit_size >= 1048576 %][% arrondi(deposit_size / 1048576) %] Mo[% END %][% IF deposit_size < 1048576 %][% arrondi(deposit_size / 1024) %] ko[% END %]</td>
				<td>[% date.format(created_date, '%d %b %Y %Hh%M') %]</td>						
				<td>[% date.format(expiration_date, '%d %b %Y %Hh%M') %]</td>
				<td>[% FOREACH download IN deposit.files.downloads %][% IF loop.last %][% loop.count %][% END %][% END %]</td>
				<td>
					[% IF deposit.id_status.id_status == 1 %]
					<a class="UIbutton ui-icon ui-icon-search" href="[% request.uri_base %]/admin/file/[% deposit.download_code %]"></a> 
					<a class="UIbutton ui-icon ui-icon-trash" href="[% request.uri_base %]/admin/file/supprimerFile/[% deposit.download_code %]"></a> 
					[% END %]
				</td>
			</tr>
			[% END %]
			</tbody>	
		</table>
[% END %]